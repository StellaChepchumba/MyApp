<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leaves">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LEAVES">
        <activity android:name=".ovalshape2"></activity>
        <activity android:name=".ablong" />
        <activity android:name=".round" />
        <activity android:name=".heartshape" />
        <activity android:name=".longshape" />
        <activity android:name=".ovalshape" />
        <activity android:name=".cordateshape" />
        <activity android:name=".heartshaped" />
        <activity android:name=".ablongshape" />
        <activity android:name=".longshaped" />
        <activity android:name=".roundshape" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>